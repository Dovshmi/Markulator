export default function Logo() {
  return (
    <div
      className="logo-mark"
      style={{
        overflow: 'hidden',
        borderRadius: 18,
        padding: 0,
        background: '#158aa4',
      }}
      aria-label="Markulator logo"
    >
      <span style={{ fontSize: 20, fontWeight: 950, letterSpacing: '-0.08em' }}>
        MS
      </span>
    </div>
  );
}
